# Secrets / local env — never commit real values (the template .env.example is kept).
.env
.env.*
!.env.example
# Per-environment connection strings for laptop-run ops (make migrate ENV=…).
secrets/

# Logs
*.log

# OS junk
.DS_Store

Scratch.md